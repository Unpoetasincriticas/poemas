<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Poemas</title>
    <style>
        /* Estilo general para el body */
        body {
            font-family: 'Georgia', serif;
            background-color: #f9f9f9;
            color: #333;
            line-height: 1.6;
            padding: 2em 1em;
        }

        /* Título principal */
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 1.5em;
        }

        /* Estilo para los contenedores de los poemas */
        .poema {
            background-color: white;
            padding: 1.5em;
            margin: 1em auto;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 800px;
        }

        /* Título de cada poema */
        .poema h2 {
            font-size: 1.5em;
            color: #444;
            margin-bottom: 0.8em;
        }

        /* Estilo para el contenido del poema */
        .poema p {
            font-size: 1.2em;
            color: #555;
            text-align: justify;
        }

        /* Estilo para la parte inferior de cada poema */
        .poema-footer {
            text-align: right;
            font-style: italic;
            font-size: 0.9em;
            color: #777;
        }

        /* Estilo para el botón de añadir poema */
        .add-poem-button {
            display: block;
            width: 200px;
            margin: 2em auto;
            padding: 0.8em;
            background-color: #007BFF;
            color: white;
            text-align: center;
            font-size: 1.1em;
            border-radius: 5px;
            text-decoration: none;
        }

        .add-poem-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<bod
